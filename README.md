# DCI Launcher

Python/Tkinter replacement for the legacy SimpleAppLauncher.exe used to launch qbase.exe per production line.

## Build

```
pyinstaller --onefile --windowed --name "DCI Launcher" -m dci_launcher
```

## Run from source

```
python -m dci_launcher
```
